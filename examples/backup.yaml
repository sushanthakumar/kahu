apiVersion: kahu.io/v1
kind: Backup
metadata:
  name: backup-demo-1
spec:
  # includedResources:
  excludedResources: []
  includedNamespaces: [default]
  metadataLocation: nfs
  includedResources:
    - name:
      kind: Deployment
      isRegex: true
    - name:
      kind: Pod
      isRegex: true
